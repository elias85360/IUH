# Roadmap

- Parameterize nginx Kienlab target via env/template
- Enable `TSDB_READ=1` with coverage for all endpoints
- Add structured JSON logging with correlation IDs
- Wire OpenTelemetry exporter (OTLP) and tracing dashboards
- UI role‑based feature gating (viewer/analyst/admin)

